{
    led_strip: ws2812 {
        compatible = "zmk,ws2812";
        pin = <&gpio0 XX>;        // 🔁 Replace XX with actual GPIO pin on right half (if different)
        num-leds = <58>;          // Or fewer, if it's split
        color-order = <1 0 2>;
        status = "okay";
    };
};
